# # Package definition for downloaded "arm-none-eabi" http-archive
# # This file describes the dowloaded package in a way Bazel will understand.

# # Make this package public across the workspace
# package(default_visibility = ["//visibility:public"])

# # Memfault - just add all files from the toolchain into a single group for now
# # accessible from @arm_none_eabi_darwin_arm64//:all
# filegroup(
#     name = "all",
#     srcs = glob(["**/*",]),
# )

